#pragma once

/**
 * Tags the nexus, so it's more easily retrievable through the registry
 * The nexus can be more efficiently be retrieved through a Map object if available
 *
 * @see Map
 */
struct Nexus{};